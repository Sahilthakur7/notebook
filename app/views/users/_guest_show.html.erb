<div class="jumbotron">
    <h1 class="display-3"><%= @user.name%>(<%= @user.username%>)</h1>
        <p class="lead"></p>
          <hr class="my-4">
            Doing some stalking?
              <p class="lead"> 

              <center><%= link_to "Published Notes",user_publish_notes_path(@user), class:"btn btn-info btn-lg"%></center>

</div>

<p style="text-align:center;"><%= image_tag @user.avatar.url(:medium), class: "img-cir"%></p>
<
